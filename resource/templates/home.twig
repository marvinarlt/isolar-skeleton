<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - {{ translation.native }}</title>
    <style>
      @import url(https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap);body{display:flex;justify-content:center;align-items:center;width:100%;height:100vh;margin:0;padding:0;background-color:#293241;color:#d5dfe5;font-family:Roboto,"Segeo UI",sans-serif}hr{margin-bottom:2rem;border-color:rgba(213,223,229,.1)}.centeredBox h1{font-size:32px}ul{display:flex;justify-content:space-around;margin:0;padding:0;list-style-type:none}li{padding:.8rem;background-color:rgba(0,0,0,.1);border-radius:1rem}li.active{cursor:default;text-decoration:underline}a{color:#d5dfe5;text-decoration:none}
    </style>
  </head>
  <body>
    <div class="centeredBox">
      <h1>{{ translation.homepage }}</h1>
      <hr>
      <ul>
        {% for language in translation.avaible %}
          {% if translation.locale == language %}
            <li class="active">{{ language }}</li>
          {% else %}
            <li>
              <a href="/{{ language }}">{{ language }}</a>
            </li>
          {% endif %}
        {% endfor %}
      </ul>
    </div>
  </body>
</html>